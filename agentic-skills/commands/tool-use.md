---
description: Apply the Tool Use pattern — enable LLMs to execute actions and retrieve information from external systems
---

Use the **Tool Use** skill from the agentic-skills plugin.

Read the full skill reference at `../skills/tool_use/SKILL.md` and apply the tool use pattern to help the user transform an LLM from a text generator into an agent by enabling it to execute actions and retrieve information from the real world.
