# Agents

No agents are defined for this plugin. The deck-builder skill acts as the orchestrator, routing through the 4-stage pipeline (diagnose → storyboard → content → PPTX) without requiring a separate agent definition.
