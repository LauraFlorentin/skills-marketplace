#!/usr/bin/env bash
# Project-local, non-mutating environment check generated from a reviewed template.

set -eu

PROJECT_ROOT="$(cd "$(dirname "$0")" && pwd)"
cd "$PROJECT_ROOT"

printf '%s\n' 'Checking project environment...'

# Replace these placeholders with commands already supported by the project.
# Do not install dependencies or start/stop persistent services in this script.

if [ -f "[REQUIRED_FILE]" ]; then
  printf 'ok: %s\n' "[REQUIRED_FILE]"
else
  printf 'missing: %s\n' "[REQUIRED_FILE]" >&2
  exit 1
fi

[VALIDATION_COMMAND]

printf '%s\n' 'Environment check passed.'
