# AGENTS.md

## Project

[One paragraph describing the product, primary stack, and important constraints.]

## Repository map

| Need | Path or command |
|---|---|
| Application entry point | [PATH] |
| Tests | [PATH OR COMMAND] |
| Validation | [COMMAND] |
| Architecture | [PATH] |
| Current plans or issues | [PATH OR LINK] |

## Working rules

- Read the nearest scoped instructions before changing a file.
- Preserve existing and uncommitted work; do not discard, stage, or commit it without authorization.
- Keep changes within the requested scope and follow established patterns.
- Run the smallest relevant validation during development and the full required checks before handoff.
- Verify user-visible behavior when the change affects an interface or workflow.
- Record assumptions and unresolved risks in the task's existing source of truth.

## Architecture constraints

- [Only include boundaries that are real, project-specific, and preferably checked mechanically.]

## Task-specific context

Load detailed design documents and execution plans only when the current task needs them:

- [TOPIC]: [PATH]

Keep this file concise and navigational. Put detailed procedures in their authoritative documents rather than duplicating them here.
