Read brand-studio/brand-config.md, then read brand-studio/references/web-branding.md. Create a branded HTML artifact. If $ARGUMENTS specifies a type (e.g. "landing page", "email template", "style guide", "component library"), build that. Otherwise ask what they need.

$ARGUMENTS
